{{- if .Values.serviceAccount.create }}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ include "kubeonec.serviceAccountName" . }}
imagePullSecrets:
{{- range  .Values.imagePullSecrets}}
  - name: {{ . }}
{{- end }}
{{- end }}
