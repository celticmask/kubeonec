{{- $createPvc := and (empty .Values.storage.persistentVolumeClaimName) (.Values.storage.requestedSize) }}
apiVersion: apps/v1
kind: StatefulSet
metadata:
  name: {{ include "kubeonec.fullname" . }}-server
  labels:
    version: " {{- .Values.image.tag }}"
    app: server
spec:
  serviceName: {{ include "kubeonec.fullname" . }}-service
  selector:
    matchLabels:
      app: server
  replicas: {{ .Values.replicaCount }}
  template:
    metadata:
      labels:
        app: server
    spec:
      initContainers:
        - name: volume-permissions
          image: busybox:1.35.0
          command: ["sh", "-c", "chmod a+wx /home /var/log/kubeonec || true"]
          volumeMounts:
            - name: {{ .Values.storage.volumeName }}
              mountPath: /home
      {{- if .Values.serviceAccount.create}}
      serviceAccount: {{ include "kubeonec.fullname" . }}-sa
      {{- end}}
      {{- with .Values.imagePullSecrets }}
      imagePullSecrets:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      containers:
      - name: server
        {{- if .Values.image.tag }}
        image: "{{ .Values.image.repository }}:{{ .Values.image.tag }}"
        {{- else }}
        image: "{{ .Values.image.repository }}:latest"
        {{- end }}
        imagePullPolicy: Always
        ports:
          - name: cluster
            containerPort: {{ .Values.ports.clusterManagerPort }}  
            protocol: TCP
          {{- range untilStep (int .Values.ports.workProccesStartPort) (int .Values.workProccesEndPort) 1 }}  
          - name: "wp-{{ . }}"
            containerPort: {{ . }}
            protocol: TCP
          {{- end }}
          - name: serveragent
            containerPort: {{ .Values.ports.serveragent }}
            protocol: TCP
          - name: ras
            containerPort: {{ .Values.ports.rasPort }}
            protocol: TCP
        volumeMounts:
        - name: {{ .Values.storage.volumeName }}
          mountPath: /home
        {{- if .Values.useHasp }}    
        - name: aksusb
          mountPath: /tmp/.aksusb
        {{- end }}
      volumes:
      {{- if .Values.useHasp }}
      - name: aksusb
        hostPath:
          path: /tmp/.aksusb
      {{- end }}
  {{- with .Values.storage }}
  {{- if not $createPvc }}
      - name: {{ .volumeName }}
      {{- if .persistentVolumeClaimName }}
        persistentVolumeClaim:
          claimName: {{ .persistentVolumeClaimName }}
      {{- else }}
        emptyDir: {}
      {{- end }}
  {{- else }}      
  volumeClaimTemplates:
    - metadata:
        name: {{ .volumeName }}
      {{- with .labels }}
        labels:
        {{- toYaml . | nindent 10 }}
      {{- end }}
      {{- with .annotations }}
        annotations:
        {{- toYaml . | nindent 10 }}
      {{- end }}
      spec:
        {{- with .accessModes }}
        accessModes:
          {{- toYaml . | nindent 10 }}
        {{- end }}
        {{- if .className }}
        storageClassName: {{ .className }}
        {{- end }}
        resources:
          requests:
            storage: {{ .requestedSize }}
  {{- end }}
  {{- end }}
